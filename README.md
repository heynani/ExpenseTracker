# ExpenseTracker
CLI-based Expense Tracker built in Java. Supports secure login, add/view/delete expenses, total spending calculation, and spending analytics (weekly, monthly, yearly averages). Uses file handling for data storage and runs smoothly in the command line for simple and efficient daily expense management.
